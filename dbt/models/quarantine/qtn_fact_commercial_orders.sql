-- GENERATED by scripts/generate_dbt_staging.py -- do not edit by hand.
-- Regenerate after changing the synthetic data model or defect profile.

{{ config(
    materialized='view'
) }}

with source as (
    select * from {{ source('lake_facts', 'fact_commercial_orders') }}
),

-- Stage 1: repair. Trim, case-fold, parse numbers out of
-- locale-formatted text, range-check timestamps.
cleansed as (
    select
        {{ clean_code('order_id') }} as order_id,
        {{ clean_timestamp('order_ts') }} as order_ts,
        {{ clean_integer('date_key') }} as date_key,
        {{ clean_code('customer_id') }} as customer_id,
        {{ clean_code('contract_id') }} as contract_id,
        {{ clean_code('product_id') }} as product_id,
        {{ fix_mojibake('sector') }} as sector,
        {{ fix_mojibake('segment') }} as segment,
        {{ fix_mojibake('channel') }} as channel,
        {{ correct_unit_error(clean_measure('volume_litres'), 3000000) }} as volume_litres,
        {{ clean_measure('list_price_zar') }} as list_price_zar,
        {{ clean_measure('discount_cents_per_litre') }} as discount_cents_per_litre,
        {{ clean_measure('net_price_zar') }} as net_price_zar,
        {{ clean_measure('revenue_zar') }} as revenue_zar,
        {{ clean_measure('cogs_zar') }} as cogs_zar,
        {{ clean_measure('gross_margin_zar') }} as gross_margin_zar,
        {{ fix_mojibake('order_status') }} as order_status,
        {{ clean_timestamp('requested_delivery_date') }} as requested_delivery_date,
        {{ clean_integer('payment_terms_days') }} as payment_terms_days,
        {{ clean_code('source_system_code') }} as source_system_code,
        {{ clean_timestamp('ingested_at') }} as ingested_at,
        1 as _dummy
    from source
),

-- Stage 2: assess. One boolean per rule, so a quarantined
-- row records exactly why it was rejected.
assessed as (
    select
        *,
        (order_id is not null) as _dq_order_id_present,
        (customer_id is not null) as _dq_customer_id_present,
        (product_id is not null) as _dq_product_id_present,
        (order_ts is not null) as _dq_event_timestamp_valid,
        (volume_litres >= 0) as _dq_volume_litres_non_negative,
        (volume_litres <= 3000000) as _dq_volume_litres_within_range,
        (list_price_zar >= 0) as _dq_list_price_zar_non_negative,
        (net_price_zar >= 0) as _dq_net_price_zar_non_negative,
        (revenue_zar >= 0) as _dq_revenue_zar_non_negative,
        (cogs_zar >= 0) as _dq_cogs_zar_non_negative,
        (gross_margin_zar >= 0) as _dq_gross_margin_zar_non_negative,
        (coalesce(_dq_order_id_present, false) and coalesce(_dq_customer_id_present, false) and coalesce(_dq_product_id_present, false) and coalesce(_dq_event_timestamp_valid, false) and coalesce(_dq_volume_litres_non_negative, false) and coalesce(_dq_volume_litres_within_range, false) and coalesce(_dq_list_price_zar_non_negative, false) and coalesce(_dq_net_price_zar_non_negative, false) and coalesce(_dq_revenue_zar_non_negative, false) and coalesce(_dq_cogs_zar_non_negative, false) and coalesce(_dq_gross_margin_zar_non_negative, false)) as _dq_is_valid,
        concat_ws(',',
            case when not coalesce(_dq_order_id_present, false) then 'order_id_present' end,
            case when not coalesce(_dq_customer_id_present, false) then 'customer_id_present' end,
            case when not coalesce(_dq_product_id_present, false) then 'product_id_present' end,
            case when not coalesce(_dq_event_timestamp_valid, false) then 'event_timestamp_valid' end,
            case when not coalesce(_dq_volume_litres_non_negative, false) then 'volume_litres_non_negative' end,
            case when not coalesce(_dq_volume_litres_within_range, false) then 'volume_litres_within_range' end,
            case when not coalesce(_dq_list_price_zar_non_negative, false) then 'list_price_zar_non_negative' end,
            case when not coalesce(_dq_net_price_zar_non_negative, false) then 'net_price_zar_non_negative' end,
            case when not coalesce(_dq_revenue_zar_non_negative, false) then 'revenue_zar_non_negative' end,
            case when not coalesce(_dq_cogs_zar_non_negative, false) then 'cogs_zar_non_negative' end,
            case when not coalesce(_dq_gross_margin_zar_non_negative, false) then 'gross_margin_zar_non_negative' end
        ) as _dq_failed_rules
    from cleansed
),

-- Rows that failed the contract. Kept, not dropped.
select
    'fact_commercial_orders' as _source_table,
    order_id,
    order_ts,
    date_key,
    customer_id,
    contract_id,
    product_id,
    sector,
    segment,
    channel,
    volume_litres,
    list_price_zar,
    discount_cents_per_litre,
    net_price_zar,
    revenue_zar,
    cogs_zar,
    gross_margin_zar,
    order_status,
    requested_delivery_date,
    payment_terms_days,
    source_system_code,
    ingested_at,
    _dq_failed_rules,
    {{ cleansing_layer_columns() }}
from assessed
where not _dq_is_valid
