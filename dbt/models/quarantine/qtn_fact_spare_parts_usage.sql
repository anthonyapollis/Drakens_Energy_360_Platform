-- GENERATED by scripts/generate_dbt_staging.py -- do not edit by hand.
-- Regenerate after changing the synthetic data model or defect profile.

-- Rows that failed the contract. Kept, not dropped: a silently
-- discarded row becomes an unexplainable variance months later,
-- and there is no way back from it.

select
    'fact_spare_parts_usage' as _source_table,
    parts_usage_id,
    usage_ts,
    date_key,
    work_order_id,
    asset_id,
    part_number,
    quantity_used,
    unit_cost_zar,
    total_cost_zar,
    warehouse_id,
    was_stocked_out,
    lead_time_days,
    source_system_code,
    ingested_at,
    _dq_failed_rules,
    _cleansed_at
from {{ ref('cln_fact_spare_parts_usage') }}
where not _dq_is_valid
