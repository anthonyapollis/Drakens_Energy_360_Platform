-- GENERATED by scripts/generate_dbt_staging.py -- do not edit by hand.
-- Regenerate after changing the synthetic data model or defect profile.

-- Rows that failed the contract. Kept, not dropped: a silently
-- discarded row becomes an unexplainable variance months later,
-- and there is no way back from it.

select
    'fact_finance_revenue_cost' as _source_table,
    finance_id,
    period_date,
    date_key,
    business_unit_code,
    site_id,
    cost_centre_code,
    revenue_zar,
    cogs_zar,
    gross_profit_zar,
    opex_zar,
    ebitda_zar,
    depreciation_zar,
    gross_margin_pct,
    currency_code,
    source_system_code,
    ingested_at,
    _dq_failed_rules,
    _cleansed_at
from {{ ref('cln_fact_finance_revenue_cost') }}
where not _dq_is_valid
