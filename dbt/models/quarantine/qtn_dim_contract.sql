-- GENERATED by scripts/generate_dbt_staging.py -- do not edit by hand.
-- Regenerate after changing the synthetic data model or defect profile.

-- Rows that failed the contract. Kept, not dropped: a silently
-- discarded row becomes an unexplainable variance months later,
-- and there is no way back from it.

select
    'dim_contract' as _source_table,
    contract_id,
    contract_key,
    customer_id,
    contract_type,
    start_date,
    end_date,
    committed_volume_litres,
    price_mechanism,
    discount_cents_per_litre,
    effective_from_date,
    effective_to_date,
    is_current,
    scd_version,
    record_source,
    dw_load_ts,
    dw_hash_diff,
    _dq_failed_rules,
    _cleansed_at
from {{ ref('cln_dim_contract') }}
where not _dq_is_valid
