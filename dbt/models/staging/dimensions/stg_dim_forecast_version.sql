-- GENERATED by scripts/generate_dbt_staging.py -- do not edit by hand.
-- Regenerate after changing the synthetic data model or defect profile.

{{ config(
    materialized='view'
) }}

with source as (
    select * from {{ source('lake_dimensions', 'dim_forecast_version') }}
),

-- Stage 1: repair. Trim, case-fold, parse numbers out of
-- locale-formatted text, range-check timestamps.
cleansed as (
    select
        {{ clean_code('forecast_version_code') }} as forecast_version_code,
        {{ clean_integer('forecast_version_key') }} as forecast_version_key,
        {{ fix_mojibake('forecast_type') }} as forecast_type,
        try_cast(published_flag as boolean) as published_flag,
        1 as _dummy
    from source
),

assessed as (
    select *, true as _dq_is_valid,
           cast('' as varchar) as _dq_failed_rules
    from cleansed
),

select
    forecast_version_code,
    forecast_version_key,
    forecast_type,
    published_flag,
    {{ cleansing_layer_columns() }}
from assessed
where _dq_is_valid
