-- GENERATED by scripts/generate_dbt_staging.py -- do not edit by hand.
-- Regenerate after changing the synthetic data model.

{{ config(
    materialized='view'
) }}

with source as (
    select * from {{ source('lake_dimensions', 'dim_warehouse') }}
),

deduplicated as (
    select
        *,
        row_number() over (partition by warehouse_id order by warehouse_id desc) as _row_rank
    from source
),

renamed as (
    select
        warehouse_id,
        warehouse_key,
        warehouse_name,
        linked_depot_id,
        pallet_positions,
        temperature_controlled,
        cast('{{ invocation_id }}' as varchar) as _dbt_run_id,
        {{ dbt_load_timestamp() }} as _dbt_loaded_at
    from deduplicated
    where _row_rank = 1
)

select * from renamed
