-- GENERATED by scripts/generate_dbt_staging.py -- do not edit by hand.
-- Regenerate after changing the synthetic data model.

{{ config(
    materialized='view'
) }}

with source as (
    select * from {{ source('lake_facts', 'fact_terminal_issues') }}
),

deduplicated as (
    select
        *,
        row_number() over (partition by terminal_issue_id order by issue_ts desc) as _row_rank
    from source
),

renamed as (
    select
        terminal_issue_id,
        issue_ts,
        date_key,
        terminal_id,
        product_id,
        vehicle_id,
        gantry_bay,
        issued_litres,
        loading_minutes,
        queue_minutes,
        destination_site_id,
        source_system_code,
        ingested_at,
        cast('{{ invocation_id }}' as varchar) as _dbt_run_id,
        {{ dbt_load_timestamp() }} as _dbt_loaded_at
    from deduplicated
    where _row_rank = 1
)

select * from renamed
