-- GENERATED by scripts/generate_dbt_staging.py -- do not edit by hand.
-- Regenerate after changing the synthetic data model or defect profile.

-- Stage 2 of 2: the clean, conformed rows the marts consume.

-- Source systems replay batches, so the same business key can
-- arrive more than once. Keep the most recently ingested version.
with deduplicated as (
    select
        *,
        row_number() over (partition by environmental_event_id order by ingested_at desc nulls last) as _row_rank
    from {{ ref('cln_fact_environmental_events') }}
    where _dq_is_valid
)

select
    environmental_event_id,
    event_ts,
    date_key,
    site_id,
    terminal_id,
    event_type,
    volume_released_litres,
    contained_flag,
    soil_affected_m2,
    remediation_cost_zar,
    regulator_notified,
    days_to_remediate,
    source_system_code,
    ingested_at,
    _cleansed_at
from deduplicated
where _row_rank = 1
