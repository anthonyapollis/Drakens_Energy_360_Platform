-- GENERATED by scripts/generate_dbt_staging.py -- do not edit by hand.
-- Regenerate after changing the synthetic data model.

{{ config(
    materialized='view'
) }}

with source as (
    select * from {{ source('lake_facts', 'fact_deliveries') }}
),

deduplicated as (
    select
        *,
        row_number() over (partition by delivery_id order by planned_arrival_ts desc) as _row_rank
    from source
),

renamed as (
    select
        delivery_id,
        planned_arrival_ts,
        actual_arrival_ts,
        date_key,
        route_id,
        origin_terminal_id,
        destination_site_id,
        vehicle_id,
        driver_id,
        carrier_id,
        product_id,
        planned_litres,
        delivered_litres,
        fill_rate_pct,
        distance_km,
        drop_count,
        delay_minutes,
        is_late,
        otif_flag,
        delivery_status,
        reason_code,
        source_system_code,
        ingested_at,
        cast('{{ invocation_id }}' as varchar) as _dbt_run_id,
        {{ dbt_load_timestamp() }} as _dbt_loaded_at
    from deduplicated
    where _row_rank = 1
)

select * from renamed
