-- GENERATED by scripts/generate_dbt_staging.py -- do not edit by hand.
-- Regenerate after changing the synthetic data model or defect profile.

-- Stage 2 of 2: the clean, conformed rows the marts consume.

-- Source systems replay batches, so the same business key can
-- arrive more than once. Keep the most recently ingested version.
with deduplicated as (
    select
        *,
        row_number() over (partition by delivery_id order by ingested_at desc nulls last) as _row_rank
    from {{ ref('cln_fact_deliveries') }}
    where _dq_is_valid
)

select
    delivery_id,
    planned_arrival_ts,
    actual_arrival_ts,
    date_key,
    route_id,
    origin_terminal_id,
    destination_site_id,
    vehicle_id,
    driver_id,
    carrier_id,
    product_id,
    planned_litres,
    delivered_litres,
    fill_rate_pct,
    distance_km,
    drop_count,
    delay_minutes,
    is_late,
    otif_flag,
    delivery_status,
    reason_code,
    source_system_code,
    ingested_at,
    _cleansed_at
from deduplicated
where _row_rank = 1
