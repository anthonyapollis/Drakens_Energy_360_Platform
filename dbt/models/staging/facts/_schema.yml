# GENERATED by scripts/generate_dbt_staging.py -- do not edit by hand.
version: 2

models:
  - name: stg_fact_accounts_payable
    description: "Cleansed fact_accounts_payable. Repaired, validated and deduplicated; rejected rows are in qtn_fact_accounts_payable."
    columns:
      - name: ap_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_accounts_receivable
    description: "Cleansed fact_accounts_receivable. Repaired, validated and deduplicated; rejected rows are in qtn_fact_accounts_receivable."
    columns:
      - name: ar_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_aircraft_uplifts
    description: "Cleansed fact_aircraft_uplifts. Repaired, validated and deduplicated; rejected rows are in qtn_fact_aircraft_uplifts."
    columns:
      - name: uplift_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_asset_downtime
    description: "Cleansed fact_asset_downtime. Repaired, validated and deduplicated; rejected rows are in qtn_fact_asset_downtime."
    columns:
      - name: downtime_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_asset_failures
    description: "Cleansed fact_asset_failures. Repaired, validated and deduplicated; rejected rows are in qtn_fact_asset_failures."
    columns:
      - name: failure_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_asset_inspections
    description: "Cleansed fact_asset_inspections. Repaired, validated and deduplicated; rejected rows are in qtn_fact_asset_inspections."
    columns:
      - name: inspection_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_asset_meter_readings
    description: "Cleansed fact_asset_meter_readings. Repaired, validated and deduplicated; rejected rows are in qtn_fact_asset_meter_readings."
    columns:
      - name: meter_reading_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_aviation_inventory_snapshot
    description: "Cleansed fact_aviation_inventory_snapshot. Repaired, validated and deduplicated; rejected rows are in qtn_fact_aviation_inventory_snapshot."
    columns:
      - name: aviation_snapshot_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_aviation_sales
    description: "Cleansed fact_aviation_sales. Repaired, validated and deduplicated; rejected rows are in qtn_fact_aviation_sales."
    columns:
      - name: aviation_sale_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_budget
    description: "Cleansed fact_budget. Repaired, validated and deduplicated; rejected rows are in qtn_fact_budget."
    columns:
      - name: budget_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_budget_vs_actual
    description: "Cleansed fact_budget_vs_actual. Repaired, validated and deduplicated; rejected rows are in qtn_fact_budget_vs_actual."
    columns:
      - name: bva_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_bunker_deliveries
    description: "Cleansed fact_bunker_deliveries. Repaired, validated and deduplicated; rejected rows are in qtn_fact_bunker_deliveries."
    columns:
      - name: bunker_delivery_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_campaign_responses
    description: "Cleansed fact_campaign_responses. Repaired, validated and deduplicated; rejected rows are in qtn_fact_campaign_responses."
    columns:
      - name: campaign_response_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_capex
    description: "Cleansed fact_capex. Repaired, validated and deduplicated; rejected rows are in qtn_fact_capex."
    columns:
      - name: capex_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_cashflow
    description: "Cleansed fact_cashflow. Repaired, validated and deduplicated; rejected rows are in qtn_fact_cashflow."
    columns:
      - name: cashflow_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_commercial_deliveries
    description: "Cleansed fact_commercial_deliveries. Repaired, validated and deduplicated; rejected rows are in qtn_fact_commercial_deliveries."
    columns:
      - name: commercial_delivery_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_commercial_order_lines
    description: "Cleansed fact_commercial_order_lines. Repaired, validated and deduplicated; rejected rows are in qtn_fact_commercial_order_lines."
    columns:
      - name: order_line_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_commercial_orders
    description: "Cleansed fact_commercial_orders. Repaired, validated and deduplicated; rejected rows are in qtn_fact_commercial_orders."
    columns:
      - name: order_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_contract_pricing
    description: "Cleansed fact_contract_pricing. Repaired, validated and deduplicated; rejected rows are in qtn_fact_contract_pricing."
    columns:
      - name: contract_price_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_contract_volume_commitments
    description: "Cleansed fact_contract_volume_commitments. Repaired, validated and deduplicated; rejected rows are in qtn_fact_contract_volume_commitments."
    columns:
      - name: commitment_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_customer_credit_exposure
    description: "Cleansed fact_customer_credit_exposure. Repaired, validated and deduplicated; rejected rows are in qtn_fact_customer_credit_exposure."
    columns:
      - name: exposure_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_customer_invoices
    description: "Cleansed fact_customer_invoices. Repaired, validated and deduplicated; rejected rows are in qtn_fact_customer_invoices."
    columns:
      - name: invoice_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_customer_receipts
    description: "Cleansed fact_customer_receipts. Repaired, validated and deduplicated; rejected rows are in qtn_fact_customer_receipts."
    columns:
      - name: receipt_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_customer_service_cases
    description: "Cleansed fact_customer_service_cases. Repaired, validated and deduplicated; rejected rows are in qtn_fact_customer_service_cases."
    columns:
      - name: case_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_daily_executive_kpi
    description: "Cleansed fact_daily_executive_kpi. Repaired, validated and deduplicated; rejected rows are in qtn_fact_daily_executive_kpi."
    columns:
      - name: executive_kpi_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_deliveries
    description: "Cleansed fact_deliveries. Repaired, validated and deduplicated; rejected rows are in qtn_fact_deliveries."
    columns:
      - name: delivery_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_delivery_events
    description: "Cleansed fact_delivery_events. Repaired, validated and deduplicated; rejected rows are in qtn_fact_delivery_events."
    columns:
      - name: delivery_event_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_digital_events
    description: "Cleansed fact_digital_events. Repaired, validated and deduplicated; rejected rows are in qtn_fact_digital_events."
    columns:
      - name: digital_event_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_digital_sessions
    description: "Cleansed fact_digital_sessions. Repaired, validated and deduplicated; rejected rows are in qtn_fact_digital_sessions."
    columns:
      - name: session_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_energy_costs
    description: "Cleansed fact_energy_costs. Repaired, validated and deduplicated; rejected rows are in qtn_fact_energy_costs."
    columns:
      - name: energy_cost_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_environmental_events
    description: "Cleansed fact_environmental_events. Repaired, validated and deduplicated; rejected rows are in qtn_fact_environmental_events."
    columns:
      - name: environmental_event_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_ev_charger_status
    description: "Cleansed fact_ev_charger_status. Repaired, validated and deduplicated; rejected rows are in qtn_fact_ev_charger_status."
    columns:
      - name: charger_status_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_ev_charging_sessions
    description: "Cleansed fact_ev_charging_sessions. Repaired, validated and deduplicated; rejected rows are in qtn_fact_ev_charging_sessions."
    columns:
      - name: ev_session_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_finance_revenue_cost
    description: "Cleansed fact_finance_revenue_cost. Repaired, validated and deduplicated; rejected rows are in qtn_fact_finance_revenue_cost."
    columns:
      - name: finance_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_fleet_costs
    description: "Cleansed fact_fleet_costs. Repaired, validated and deduplicated; rejected rows are in qtn_fact_fleet_costs."
    columns:
      - name: fleet_cost_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_forecast
    description: "Cleansed fact_forecast. Repaired, validated and deduplicated; rejected rows are in qtn_fact_forecast."
    columns:
      - name: forecast_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_forecourt_shift
    description: "Cleansed fact_forecourt_shift. Repaired, validated and deduplicated; rejected rows are in qtn_fact_forecourt_shift."
    columns:
      - name: forecourt_shift_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_general_ledger
    description: "Cleansed fact_general_ledger. Repaired, validated and deduplicated; rejected rows are in qtn_fact_general_ledger."
    columns:
      - name: journal_line_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_grid_import_export
    description: "Cleansed fact_grid_import_export. Repaired, validated and deduplicated; rejected rows are in qtn_fact_grid_import_export."
    columns:
      - name: grid_flow_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_hsseq_incidents
    description: "Cleansed fact_hsseq_incidents. Repaired, validated and deduplicated; rejected rows are in qtn_fact_hsseq_incidents."
    columns:
      - name: incident_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_import_cargoes
    description: "Cleansed fact_import_cargoes. Repaired, validated and deduplicated; rejected rows are in qtn_fact_import_cargoes."
    columns:
      - name: cargo_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_inventory_adjustments
    description: "Cleansed fact_inventory_adjustments. Repaired, validated and deduplicated; rejected rows are in qtn_fact_inventory_adjustments."
    columns:
      - name: adjustment_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_inventory_movements
    description: "Cleansed fact_inventory_movements. Repaired, validated and deduplicated; rejected rows are in qtn_fact_inventory_movements."
    columns:
      - name: movement_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_inventory_snapshot
    description: "Cleansed fact_inventory_snapshot. Repaired, validated and deduplicated; rejected rows are in qtn_fact_inventory_snapshot."
    columns:
      - name: snapshot_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_loyalty_points_balance
    description: "Cleansed fact_loyalty_points_balance. Repaired, validated and deduplicated; rejected rows are in qtn_fact_loyalty_points_balance."
    columns:
      - name: points_balance_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_loyalty_transactions
    description: "Cleansed fact_loyalty_transactions. Repaired, validated and deduplicated; rejected rows are in qtn_fact_loyalty_transactions."
    columns:
      - name: loyalty_transaction_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_lpg_bulk_deliveries
    description: "Cleansed fact_lpg_bulk_deliveries. Repaired, validated and deduplicated; rejected rows are in qtn_fact_lpg_bulk_deliveries."
    columns:
      - name: lpg_delivery_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_lpg_cylinder_movements
    description: "Cleansed fact_lpg_cylinder_movements. Repaired, validated and deduplicated; rejected rows are in qtn_fact_lpg_cylinder_movements."
    columns:
      - name: cylinder_movement_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_lpg_inventory_snapshot
    description: "Cleansed fact_lpg_inventory_snapshot. Repaired, validated and deduplicated; rejected rows are in qtn_fact_lpg_inventory_snapshot."
    columns:
      - name: lpg_snapshot_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_lpg_sales
    description: "Cleansed fact_lpg_sales. Repaired, validated and deduplicated; rejected rows are in qtn_fact_lpg_sales."
    columns:
      - name: lpg_sale_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_lubricant_inventory_snapshot
    description: "Cleansed fact_lubricant_inventory_snapshot. Repaired, validated and deduplicated; rejected rows are in qtn_fact_lubricant_inventory_snapshot."
    columns:
      - name: lubricant_snapshot_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_lubricant_orders
    description: "Cleansed fact_lubricant_orders. Repaired, validated and deduplicated; rejected rows are in qtn_fact_lubricant_orders."
    columns:
      - name: lubricant_order_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_lubricant_sales
    description: "Cleansed fact_lubricant_sales. Repaired, validated and deduplicated; rejected rows are in qtn_fact_lubricant_sales."
    columns:
      - name: lubricant_sale_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_maintenance_work_orders
    description: "Cleansed fact_maintenance_work_orders. Repaired, validated and deduplicated; rejected rows are in qtn_fact_maintenance_work_orders."
    columns:
      - name: work_order_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_marine_inventory_snapshot
    description: "Cleansed fact_marine_inventory_snapshot. Repaired, validated and deduplicated; rejected rows are in qtn_fact_marine_inventory_snapshot."
    columns:
      - name: marine_snapshot_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_marine_sales
    description: "Cleansed fact_marine_sales. Repaired, validated and deduplicated; rejected rows are in qtn_fact_marine_sales."
    columns:
      - name: marine_sale_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_mobile_payments
    description: "Cleansed fact_mobile_payments. Repaired, validated and deduplicated; rejected rows are in qtn_fact_mobile_payments."
    columns:
      - name: mobile_payment_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_near_misses
    description: "Cleansed fact_near_misses. Repaired, validated and deduplicated; rejected rows are in qtn_fact_near_misses."
    columns:
      - name: near_miss_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_opex
    description: "Cleansed fact_opex. Repaired, validated and deduplicated; rejected rows are in qtn_fact_opex."
    columns:
      - name: opex_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_permit_to_work
    description: "Cleansed fact_permit_to_work. Repaired, validated and deduplicated; rejected rows are in qtn_fact_permit_to_work."
    columns:
      - name: permit_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_price_changes
    description: "Cleansed fact_price_changes. Repaired, validated and deduplicated; rejected rows are in qtn_fact_price_changes."
    columns:
      - name: price_change_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_procurement_orders
    description: "Cleansed fact_procurement_orders. Repaired, validated and deduplicated; rejected rows are in qtn_fact_procurement_orders."
    columns:
      - name: purchase_order_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_procurement_receipts
    description: "Cleansed fact_procurement_receipts. Repaired, validated and deduplicated; rejected rows are in qtn_fact_procurement_receipts."
    columns:
      - name: receipt_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_promotions
    description: "Cleansed fact_promotions. Repaired, validated and deduplicated; rejected rows are in qtn_fact_promotions."
    columns:
      - name: promotion_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_pump_meter_readings
    description: "Cleansed fact_pump_meter_readings. Repaired, validated and deduplicated; rejected rows are in qtn_fact_pump_meter_readings."
    columns:
      - name: reading_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_replenishment_orders
    description: "Cleansed fact_replenishment_orders. Repaired, validated and deduplicated; rejected rows are in qtn_fact_replenishment_orders."
    columns:
      - name: replenishment_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_retail_fuel_payments
    description: "Cleansed fact_retail_fuel_payments. Repaired, validated and deduplicated; rejected rows are in qtn_fact_retail_fuel_payments."
    columns:
      - name: payment_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_retail_fuel_sales
    description: "Cleansed fact_retail_fuel_sales. Repaired, validated and deduplicated; rejected rows are in qtn_fact_retail_fuel_sales."
    columns:
      - name: transaction_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_route_performance
    description: "Cleansed fact_route_performance. Repaired, validated and deduplicated; rejected rows are in qtn_fact_route_performance."
    columns:
      - name: route_performance_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_safety_observations
    description: "Cleansed fact_safety_observations. Repaired, validated and deduplicated; rejected rows are in qtn_fact_safety_observations."
    columns:
      - name: observation_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_shop_returns
    description: "Cleansed fact_shop_returns. Repaired, validated and deduplicated; rejected rows are in qtn_fact_shop_returns."
    columns:
      - name: return_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_shop_sales
    description: "Cleansed fact_shop_sales. Repaired, validated and deduplicated; rejected rows are in qtn_fact_shop_sales."
    columns:
      - name: shop_line_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_site_daily_operations
    description: "Cleansed fact_site_daily_operations. Repaired, validated and deduplicated; rejected rows are in qtn_fact_site_daily_operations."
    columns:
      - name: site_day_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_site_energy_consumption
    description: "Cleansed fact_site_energy_consumption. Repaired, validated and deduplicated; rejected rows are in qtn_fact_site_energy_consumption."
    columns:
      - name: energy_consumption_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_solar_generation
    description: "Cleansed fact_solar_generation. Repaired, validated and deduplicated; rejected rows are in qtn_fact_solar_generation."
    columns:
      - name: solar_reading_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_spare_parts_usage
    description: "Cleansed fact_spare_parts_usage. Repaired, validated and deduplicated; rejected rows are in qtn_fact_spare_parts_usage."
    columns:
      - name: parts_usage_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_stock_losses
    description: "Cleansed fact_stock_losses. Repaired, validated and deduplicated; rejected rows are in qtn_fact_stock_losses."
    columns:
      - name: stock_loss_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_tank_dips
    description: "Cleansed fact_tank_dips. Repaired, validated and deduplicated; rejected rows are in qtn_fact_tank_dips."
    columns:
      - name: tank_dip_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_terminal_issues
    description: "Cleansed fact_terminal_issues. Repaired, validated and deduplicated; rejected rows are in qtn_fact_terminal_issues."
    columns:
      - name: terminal_issue_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_terminal_receipts
    description: "Cleansed fact_terminal_receipts. Repaired, validated and deduplicated; rejected rows are in qtn_fact_terminal_receipts."
    columns:
      - name: terminal_receipt_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_terminal_throughput
    description: "Cleansed fact_terminal_throughput. Repaired, validated and deduplicated; rejected rows are in qtn_fact_terminal_throughput."
    columns:
      - name: throughput_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_training_compliance
    description: "Cleansed fact_training_compliance. Repaired, validated and deduplicated; rejected rows are in qtn_fact_training_compliance."
    columns:
      - name: training_record_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_vehicle_safety_events
    description: "Cleansed fact_vehicle_safety_events. Repaired, validated and deduplicated; rejected rows are in qtn_fact_vehicle_safety_events."
    columns:
      - name: vehicle_safety_event_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_vehicle_trips
    description: "Cleansed fact_vehicle_trips. Repaired, validated and deduplicated; rejected rows are in qtn_fact_vehicle_trips."
    columns:
      - name: trip_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
  - name: stg_fact_working_capital
    description: "Cleansed fact_working_capital. Repaired, validated and deduplicated; rejected rows are in qtn_fact_working_capital."
    columns:
      - name: working_capital_id
        description: "Business key. Unique and not null after cleansing -- if this fails, the cleansing layer is letting bad rows through."
        data_tests:
          - unique
          - not_null
