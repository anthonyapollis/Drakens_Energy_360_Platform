-- GENERATED by scripts/generate_dbt_staging.py -- do not edit by hand.
-- Regenerate after changing the synthetic data model.

{{ config(
    materialized='view'
) }}

with source as (
    select * from {{ source('lake_facts', 'fact_shop_returns') }}
),

deduplicated as (
    select
        *,
        row_number() over (partition by return_id order by return_ts desc) as _row_rank
    from source
),

renamed as (
    select
        return_id,
        return_ts,
        date_key,
        site_id,
        product_id,
        quantity_returned,
        refund_value_zar,
        reason_code,
        approved_by_employee_id,
        source_system_code,
        ingested_at,
        cast('{{ invocation_id }}' as varchar) as _dbt_run_id,
        {{ dbt_load_timestamp() }} as _dbt_loaded_at
    from deduplicated
    where _row_rank = 1
)

select * from renamed
