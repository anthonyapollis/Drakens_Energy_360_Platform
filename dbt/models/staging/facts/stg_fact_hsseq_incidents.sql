-- GENERATED by scripts/generate_dbt_staging.py -- do not edit by hand.
-- Regenerate after changing the synthetic data model or defect profile.

-- Stage 2 of 2: the clean, conformed rows the marts consume.

-- Source systems replay batches, so the same business key can
-- arrive more than once. Keep the most recently ingested version.
with deduplicated as (
    select
        *,
        row_number() over (partition by incident_id order by ingested_at desc nulls last) as _row_rank
    from {{ ref('cln_fact_hsseq_incidents') }}
    where _dq_is_valid
)

select
    incident_id,
    incident_ts,
    date_key,
    site_id,
    incident_type,
    severity,
    severity_rank,
    is_lost_time_injury,
    lost_days,
    people_affected,
    estimated_cost_zar,
    is_reportable,
    root_cause_category,
    corrective_actions_raised,
    corrective_actions_closed,
    investigation_days,
    reported_by_employee_id,
    source_system_code,
    ingested_at,
    _cleansed_at
from deduplicated
where _row_rank = 1
