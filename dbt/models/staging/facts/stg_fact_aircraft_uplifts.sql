-- GENERATED by scripts/generate_dbt_staging.py -- do not edit by hand.
-- Regenerate after changing the synthetic data model or defect profile.

-- Stage 2 of 2: the clean, conformed rows the marts consume.

-- Source systems replay batches, so the same business key can
-- arrive more than once. Keep the most recently ingested version.
with deduplicated as (
    select
        *,
        row_number() over (partition by uplift_id order by ingested_at desc nulls last) as _row_rank
    from {{ ref('cln_fact_aircraft_uplifts') }}
    where _dq_is_valid
)

select
    uplift_id,
    uplift_ts,
    date_key,
    airport_code,
    airline_code,
    flight_number,
    aircraft_registration,
    uplift_litres,
    uplift_duration_minutes,
    density_kg_per_litre,
    meter_start,
    quality_check_passed,
    source_system_code,
    ingested_at,
    _cleansed_at
from deduplicated
where _row_rank = 1
