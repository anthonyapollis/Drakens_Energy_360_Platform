-- GENERATED by scripts/generate_dbt_staging.py -- do not edit by hand.
-- Regenerate after changing the synthetic data model.

{{ config(
    materialized='view'
) }}

with source as (
    select * from {{ source('lake_facts', 'fact_campaign_responses') }}
),

deduplicated as (
    select
        *,
        row_number() over (partition by campaign_response_id order by response_ts desc) as _row_rank
    from source
),

renamed as (
    select
        campaign_response_id,
        response_ts,
        date_key,
        campaign_id,
        digital_user_id,
        channel,
        was_delivered,
        was_opened,
        was_clicked,
        was_redeemed,
        incremental_spend_zar,
        attribution_model,
        source_system_code,
        ingested_at,
        cast('{{ invocation_id }}' as varchar) as _dbt_run_id,
        {{ dbt_load_timestamp() }} as _dbt_loaded_at
    from deduplicated
    where _row_rank = 1
)

select * from renamed
