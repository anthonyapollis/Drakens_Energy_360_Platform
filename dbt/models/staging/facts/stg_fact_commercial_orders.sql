-- GENERATED by scripts/generate_dbt_staging.py -- do not edit by hand.
-- Regenerate after changing the synthetic data model.

{{ config(
    materialized='view'
) }}

with source as (
    select * from {{ source('lake_facts', 'fact_commercial_orders') }}
),

deduplicated as (
    select
        *,
        row_number() over (partition by order_id order by order_ts desc) as _row_rank
    from source
),

renamed as (
    select
        order_id,
        order_ts,
        date_key,
        customer_id,
        contract_id,
        product_id,
        sector,
        segment,
        channel,
        volume_litres,
        list_price_zar,
        discount_cents_per_litre,
        net_price_zar,
        revenue_zar,
        cogs_zar,
        gross_margin_zar,
        order_status,
        requested_delivery_date,
        payment_terms_days,
        source_system_code,
        ingested_at,
        cast('{{ invocation_id }}' as varchar) as _dbt_run_id,
        {{ dbt_load_timestamp() }} as _dbt_loaded_at
    from deduplicated
    where _row_rank = 1
)

select * from renamed
