-- GENERATED by scripts/generate_dbt_staging.py -- do not edit by hand.
-- Regenerate after changing the synthetic data model or defect profile.

-- Stage 2 of 2: the clean, conformed rows the marts consume.

-- Source systems replay batches, so the same business key can
-- arrive more than once. Keep the most recently ingested version.
with deduplicated as (
    select
        *,
        row_number() over (partition by permit_id order by ingested_at desc nulls last) as _row_rank
    from {{ ref('cln_fact_permit_to_work') }}
    where _dq_is_valid
)

select
    permit_id,
    issued_ts,
    date_key,
    site_id,
    permit_type,
    contractor_flag,
    issued_by_employee_id,
    planned_duration_hours,
    closed_ts,
    isolations_required,
    gas_test_performed,
    closed_correctly,
    source_system_code,
    ingested_at,
    _cleansed_at
from deduplicated
where _row_rank = 1
