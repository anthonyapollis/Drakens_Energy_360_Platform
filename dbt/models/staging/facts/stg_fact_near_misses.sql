-- GENERATED by scripts/generate_dbt_staging.py -- do not edit by hand.
-- Regenerate after changing the synthetic data model or defect profile.

-- Stage 2 of 2: the clean, conformed rows the marts consume.

-- Source systems replay batches, so the same business key can
-- arrive more than once. Keep the most recently ingested version.
with deduplicated as (
    select
        *,
        row_number() over (partition by near_miss_id order by ingested_at desc nulls last) as _row_rank
    from {{ ref('cln_fact_near_misses') }}
    where _dq_is_valid
)

select
    near_miss_id,
    reported_ts,
    date_key,
    site_id,
    hazard_type,
    potential_severity,
    immediate_action_taken,
    days_to_close,
    reported_by_employee_id,
    is_repeat_hazard,
    source_system_code,
    ingested_at,
    _cleansed_at
from deduplicated
where _row_rank = 1
