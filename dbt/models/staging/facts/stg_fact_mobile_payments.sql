-- GENERATED by scripts/generate_dbt_staging.py -- do not edit by hand.
-- Regenerate after changing the synthetic data model.

{{ config(
    materialized='view'
) }}

with source as (
    select * from {{ source('lake_facts', 'fact_mobile_payments') }}
),

deduplicated as (
    select
        *,
        row_number() over (partition by mobile_payment_id order by payment_ts desc) as _row_rank
    from source
),

renamed as (
    select
        mobile_payment_id,
        payment_ts,
        date_key,
        digital_user_id,
        site_id,
        amount_zar,
        payment_instrument,
        is_approved,
        decline_reason,
        processing_ms,
        is_flagged_fraud,
        source_system_code,
        ingested_at,
        cast('{{ invocation_id }}' as varchar) as _dbt_run_id,
        {{ dbt_load_timestamp() }} as _dbt_loaded_at
    from deduplicated
    where _row_rank = 1
)

select * from renamed
