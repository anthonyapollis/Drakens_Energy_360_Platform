-- GENERATED by scripts/generate_dbt_staging.py -- do not edit by hand.
-- Regenerate after changing the synthetic data model.

{{ config(
    materialized='view'
) }}

with source as (
    select * from {{ source('lake_facts', 'fact_promotions') }}
),

deduplicated as (
    select
        *,
        row_number() over (partition by promotion_id order by measurement_date desc) as _row_rank
    from source
),

renamed as (
    select
        promotion_fact_id,
        measurement_date,
        date_key,
        site_id,
        promotion_id,
        baseline_units,
        promoted_units,
        uplift_pct,
        discount_cost_zar,
        incremental_margin_zar,
        source_system_code,
        ingested_at,
        cast('{{ invocation_id }}' as varchar) as _dbt_run_id,
        {{ dbt_load_timestamp() }} as _dbt_loaded_at
    from deduplicated
    where _row_rank = 1
)

select * from renamed
