-- GENERATED by scripts/generate_dbt_staging.py -- do not edit by hand.
-- Regenerate after changing the synthetic data model.

{{ config(
    materialized='view'
) }}

with source as (
    select * from {{ source('lake_facts', 'fact_digital_sessions') }}
),

deduplicated as (
    select
        *,
        row_number() over (partition by session_id order by session_start_ts desc) as _row_rank
    from source
),

renamed as (
    select
        session_id,
        session_start_ts,
        session_end_ts,
        date_key,
        digital_user_id,
        device_type,
        event_count,
        duration_seconds,
        screens_viewed,
        is_bounce,
        converted,
        acquisition_channel,
        source_system_code,
        ingested_at,
        cast('{{ invocation_id }}' as varchar) as _dbt_run_id,
        {{ dbt_load_timestamp() }} as _dbt_loaded_at
    from deduplicated
    where _row_rank = 1
)

select * from renamed
