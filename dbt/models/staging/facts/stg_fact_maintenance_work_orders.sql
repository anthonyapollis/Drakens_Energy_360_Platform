-- GENERATED by scripts/generate_dbt_staging.py -- do not edit by hand.
-- Regenerate after changing the synthetic data model or defect profile.

-- Stage 2 of 2: the clean, conformed rows the marts consume.

-- Source systems replay batches, so the same business key can
-- arrive more than once. Keep the most recently ingested version.
with deduplicated as (
    select
        *,
        row_number() over (partition by work_order_id order by ingested_at desc nulls last) as _row_rank
    from {{ ref('cln_fact_maintenance_work_orders') }}
    where _dq_is_valid
)

select
    work_order_id,
    raised_ts,
    date_key,
    asset_id,
    site_id,
    asset_type,
    criticality,
    asset_age_years,
    maintenance_type,
    failure_mode,
    priority,
    response_hours,
    labour_hours,
    labour_cost_zar,
    parts_cost_zar,
    total_cost_zar,
    completed_ts,
    is_completed,
    is_breakdown,
    technician_employee_id,
    sla_breached,
    source_system_code,
    ingested_at,
    _cleansed_at
from deduplicated
where _row_rank = 1
