-- GENERATED by scripts/generate_dbt_staging.py -- do not edit by hand.
-- Regenerate after changing the synthetic data model.

{{ config(
    materialized='view'
) }}

with source as (
    select * from {{ source('lake_facts', 'fact_marine_sales') }}
),

deduplicated as (
    select
        *,
        row_number() over (partition by marine_sale_id order by sale_ts desc) as _row_rank
    from source
),

renamed as (
    select
        marine_sale_id,
        sale_ts,
        date_key,
        port_code,
        customer_id,
        vessel_imo,
        product_id,
        bunker_litres,
        unit_price_zar,
        revenue_zar,
        cogs_zar,
        vessel_class,
        sulphur_pct,
        source_system_code,
        ingested_at,
        cast('{{ invocation_id }}' as varchar) as _dbt_run_id,
        {{ dbt_load_timestamp() }} as _dbt_loaded_at
    from deduplicated
    where _row_rank = 1
)

select * from renamed
