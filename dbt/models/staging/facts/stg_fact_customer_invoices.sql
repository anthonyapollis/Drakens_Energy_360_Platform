-- GENERATED by scripts/generate_dbt_staging.py -- do not edit by hand.
-- Regenerate after changing the synthetic data model or defect profile.

-- Stage 2 of 2: the clean, conformed rows the marts consume.

-- Source systems replay batches, so the same business key can
-- arrive more than once. Keep the most recently ingested version.
with deduplicated as (
    select
        *,
        row_number() over (partition by invoice_id order by ingested_at desc nulls last) as _row_rank
    from {{ ref('cln_fact_customer_invoices') }}
    where _dq_is_valid
)

select
    invoice_id,
    invoice_date,
    date_key,
    customer_id,
    due_date,
    net_amount_zar,
    vat_amount_zar,
    gross_amount_zar,
    payment_terms_days,
    days_to_payment,
    is_overdue,
    credit_band,
    invoice_status,
    source_system_code,
    ingested_at,
    _cleansed_at
from deduplicated
where _row_rank = 1
