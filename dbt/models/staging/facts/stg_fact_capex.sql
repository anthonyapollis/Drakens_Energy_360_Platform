-- GENERATED by scripts/generate_dbt_staging.py -- do not edit by hand.
-- Regenerate after changing the synthetic data model or defect profile.

-- Stage 2 of 2: the clean, conformed rows the marts consume.

-- Source systems replay batches, so the same business key can
-- arrive more than once. Keep the most recently ingested version.
with deduplicated as (
    select
        *,
        row_number() over (partition by capex_id order by ingested_at desc nulls last) as _row_rank
    from {{ ref('cln_fact_capex') }}
    where _dq_is_valid
)

select
    capex_id,
    period_date,
    date_key,
    project_code,
    site_id,
    capex_category,
    budget_zar,
    actual_spend_zar,
    variance_zar,
    variance_pct,
    expected_irr_pct,
    payback_years,
    project_status,
    source_system_code,
    ingested_at,
    _cleansed_at
from deduplicated
where _row_rank = 1
