-- GENERATED by scripts/generate_dbt_staging.py -- do not edit by hand.
-- Regenerate after changing the synthetic data model or defect profile.

-- Stage 2 of 2: the clean, conformed rows the marts consume.

-- Source systems replay batches, so the same business key can
-- arrive more than once. Keep the most recently ingested version.
with deduplicated as (
    select
        *,
        row_number() over (partition by energy_cost_id order by ingested_at desc nulls last) as _row_rank
    from {{ ref('cln_fact_energy_costs') }}
    where _dq_is_valid
)

select
    energy_cost_id,
    billing_period_start,
    date_key,
    site_id,
    energy_source,
    consumption_kwh,
    unit_rate_zar_per_kwh,
    demand_charge_zar,
    energy_charge_zar,
    total_cost_zar,
    carbon_kg,
    cost_centre_code,
    source_system_code,
    ingested_at,
    _cleansed_at
from deduplicated
where _row_rank = 1
