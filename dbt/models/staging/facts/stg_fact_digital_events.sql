-- GENERATED by scripts/generate_dbt_staging.py -- do not edit by hand.
-- Regenerate after changing the synthetic data model.

{{ config(
    materialized='view'
) }}

with source as (
    select * from {{ source('lake_facts', 'fact_digital_events') }}
),

deduplicated as (
    select
        *,
        row_number() over (partition by digital_event_id order by event_ts desc) as _row_rank
    from source
),

renamed as (
    select
        digital_event_id,
        event_ts,
        date_key,
        time_key,
        digital_user_id,
        session_id,
        event_type,
        device_type,
        app_version,
        site_id,
        screen_name,
        duration_ms,
        is_conversion,
        network_type,
        source_system_code,
        ingested_at,
        cast('{{ invocation_id }}' as varchar) as _dbt_run_id,
        {{ dbt_load_timestamp() }} as _dbt_loaded_at
    from deduplicated
    where _row_rank = 1
)

select * from renamed
