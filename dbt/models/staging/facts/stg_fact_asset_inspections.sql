-- GENERATED by scripts/generate_dbt_staging.py -- do not edit by hand.
-- Regenerate after changing the synthetic data model or defect profile.

-- Stage 2 of 2: the clean, conformed rows the marts consume.

-- Source systems replay batches, so the same business key can
-- arrive more than once. Keep the most recently ingested version.
with deduplicated as (
    select
        *,
        row_number() over (partition by inspection_id order by ingested_at desc nulls last) as _row_rank
    from {{ ref('cln_fact_asset_inspections') }}
    where _dq_is_valid
)

select
    inspection_id,
    inspection_ts,
    date_key,
    asset_id,
    site_id,
    inspection_type,
    inspector_employee_id,
    result,
    defects_found,
    condition_score,
    next_due_date,
    is_overdue,
    source_system_code,
    ingested_at,
    _cleansed_at
from deduplicated
where _row_rank = 1
